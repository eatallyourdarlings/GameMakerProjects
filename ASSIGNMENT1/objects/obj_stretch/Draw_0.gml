//draw_self();

draw_sprite_ext(sprite_index,
image_index, x, y, draw_xscale,
draw_yscale, image_angle, image_blend,
image_alpha);

//draw_sprite(spr_sunglasses, 0, x, y)

if cool { //hell yeah
	draw_sprite(spr_sunglasses, 0, x, y)
}